Create local domains for all run docker containers with alias
=============

ALIAS

Add alias in aliasArr array
aliasArr[DOCKER_CONTAINER_NAME]="alias1,alias2"

As a result will be added to /etc/hosts
alias1.docker.local
alias2.docker.local

